stages:
  - build_sphinx
  - deploy

build_sphinx:
  image: python:3.7-alpine
  stage: build_sphinx
  script:
  - pip install -U sphinx
  - pip install -U sphinx-rtd-theme
  - sphinx-build -b html . public
  - sphinx-build -b latex . _latex
  artifacts:
    paths:
    - public
    - _latex
  only:
  - master

pages:
  image: aergus/latex
  stage: deploy
  script:
  - cd _latex
  - latexmk
  - mv test.pdf ../public/download/FEM.pdf
  artifacts:
    paths:
    - public
  only:
  - master
